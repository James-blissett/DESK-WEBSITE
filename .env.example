# Supabase Configuration
# REQUIRED: These are needed for the app to work
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# OPTIONAL: Only needed for admin operations (webhooks, server-side writes that bypass RLS)
# WARNING: Keep this secret! Never expose in client-side code. Found in Settings â†’ API â†’ service_role key (secret)
SUPABASE_SERVICE_ROLE_KEY=

# Stripe Configuration (OPTIONAL - only needed for payment processing)
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# Resend Configuration (OPTIONAL - only needed for sending emails)
RESEND_API_KEY=
